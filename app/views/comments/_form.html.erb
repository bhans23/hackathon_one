<%= form_for [@movie, @comment] do |f| %>
  <%= f.label :full_name %>
  <%= f.text_field :full_name, placeholder: 'Leave Blank To Remain Anonymous' %>
  <%= f.label :body %>
  <%= f.text_area :body, required: true %>
  <%= f.submit %>
<% end %>